# Bubble API Configuration
BUBBLE_BASE_URL=https://your-app.bubbleapps.io
BUBBLE_API_TOKEN=your-bubble-api-token-here

# MCP Server Configuration
# MCP_MODE controls whether write operations are allowed
# Options: read-only (default), read-write
# When unset or set to "read-only", write tools (create, update, delete, workflow) return errors
# Set to "read-write" to enable all operations
MCP_MODE=read-only

# MCP Server Configuration
MCP_SERVER_NAME=bubble-mcp-server
